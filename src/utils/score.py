from typing import Optional

from src.data_handler.data.input_data import InputData
from src.data_handler.data.output_data import OutputData


def calculate_score(input_data: InputData, output_data: OutputData) -> Optional[float]:
    """Return the local score if the contest provides a formula."""
    return None
